# EV-Trip-Optimizer
Calendar-aware EV charging for Home Assistant — calculates real trip energy needs via Google Routes, finds the cheapest charging window before each deadline, and overrides price thresholds when time is tight. Charger-agnostic: works with any EV charging hardware.
